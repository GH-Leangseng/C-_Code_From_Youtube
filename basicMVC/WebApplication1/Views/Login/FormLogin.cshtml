
@*@model List<WebApplication1.Models.clsStudent> // before we comment we use with list *@
@model WebApplication1.Models.clsMain 
@*//use list Object that are  model for using with database*@
@{
    //Layout = null; //clear layout share include HTML heander and body footer
    ViewData["Title"] = "FormLogin";

}


<div class="container">
    <h1 class="text-uppercase text-center">FormLogin</h1>
    <div class="row justify-content-center">
        <div class="col-sm-12">
            <table class="table">
                <thead>
                    <tr>
                        <td>ID</td>
                        <td>Name</td>
                        <td>Gender</td>
                    </tr>
                </thead>
                <tbody>
                    @*@{ int a = 100;}*@
                    @foreach (var item in Model.student) //model from ClsStudent every fiel wil be past to this block
                    {
                        <tr>
                            <td>@item.SID </td>
                            <td>@item.Name</td>
                            <td>@item.Gender</td>
                        </tr>
                    }
                    @foreach (var item in Model.person) //model from ClsStudent every fiel wil be past to this block
                    {
                        <tr>
                            <td>@item.PID</td>
                            <td>@item.Pname</td>
                            <td>@item.Age</td>
                        </tr>
                    }

                    @*@for (int i = 0; i < Model.Count; i++)
        {
            <tr>
                <td>@Model[i].SID</td>
                <td>@Model[i].Name</td>
                <td>@Model[i].Gender</td>
            </tr>
        }*@
                </tbody>
            </table>
        </div>

      

    </div>
</div>

